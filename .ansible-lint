---
exclude_paths:
  - .cache/
  - .github/
  - molecule/

skip_list:
  - yaml[line-length]  # Allow longer lines for readability

warn_list:
  - experimental  # Experimental rules produce warnings instead of errors

use_default_rules: true